<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="database.AccountMapper">
 	<select id="firstJoinCheck" parameterType="AccountList" resultType="AccountList">
 		SELECT * FROM userList WHERE id=#{id} AND nickname=#{nickname}
 	</select>
 	<insert id="firstJoinAccount" parameterType="AccountList">
 		INSERT INTO userList VALUES(#{id}, #{nickname}, #{address}, #{tel})
 	</insert>
 	<update id="changeAccount" parameterType="AccountList">
 		UPDATE userList SET address=#{address}, tel=#{tel} WHERE id=#{id} AND nickname=#{nickname}
 	</update>
 	<delete id="deleteAccount" parameterType="AccountList">
 		DELETE FROM userList WHERE id=#{id} AND nickname=#{nickname}
 	</delete>
 </mapper>